import {
  USER_LOAD,
} from '../constants/actionTypes';

const initialState = {
  username: undefined,
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
