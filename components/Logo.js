import React, { PropTypes, Component } from 'react';

class Logo extends Component {
  render() {
    return (
      <a href=''>
        <img src='' />
      </a>
    );
  }
}

Logo.propTypes = {
};

export default Logo;
